---
import { ClientRouter } from "astro:transitions";
import { fetchNavTop } from "@/src/utils/queries/fetchNavTop";
import { fetchFooter } from "@/src/utils/queries/fetchFooter";

import Footer from "@/src/components/common/Footer.astro";
import NavTop from "@/src/components/navs/NavTop.astro";
import Nav from "@/src/components/navs/Nav.astro";
import "@/src/styles/global.css";

const navData = await fetchNavTop();
const footerData = await fetchFooter();
const { title } = Astro.props;
---

<!doctype html>
<html lang="es" class="scroll-smooth scrollbar-hide">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/x-icon" href="/favicon.ico" />
    <title>{title}</title>
    <ClientRouter />
  </head>
  <body>
    <NavTop {...navData} />
    <Nav />
    <slot />
    <Footer {...footerData}/>
  </body>
</html>

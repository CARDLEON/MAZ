---
import LinkedIn from "@/public/linkedin.svg";
import Facebook from "@/public/facebook.svg";
import Phone from "@/public/phone.svg";
import Mail from "@/public/mail.svg";
import Clock from "@/public/clock.svg";

type Props = {
  phone_top: string;
  email_top: string;
  schedule_top: string;
  fb_link_top: string;
  in_link_top: string;
};

const { phone_top, email_top, schedule_top, fb_link_top, in_link_top } =
  Astro.props;
---

<section
  class="w-full bg-[#24408d] text-white text-[12px] z-50 px-4 sm:px-6 md:px-8 2xl:px-48 py-2"
>
  <div
    class="flex flex-col md:flex-row justify-between items-start md:items-center gap-2 md:gap-0"
  >
    <section>
      <ul
        class="flex flex-col sm:flex-row flex-wrap gap-2 sm:gap-4 items-start sm:items-center"
      >
        <li class="flex gap-2 items-center">
          <Phone width={20} />
          {phone_top}
        </li>
        <li class="flex gap-2 items-center">
          <Mail width={20} />
          {email_top}
        </li>
        <li class="flex gap-2 items-center">
          <Clock width={20} />
          {schedule_top}
        </li>
      </ul>
    </section>

    <ul class="flex gap-2 mt-2 md:mt-0">
      <li>
        <a href={fb_link_top} target="_blank">
          <Facebook width={20} />
        </a>
      </li>
      <li>
        <a href={in_link_top} target="_blank">
          <LinkedIn width={20} />
        </a>
      </li>
    </ul>
  </div>
</section>

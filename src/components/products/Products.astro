---
import ProductGrid from "@/src/components/products/ProductGrid.astro";

import { fetchAllProducts } from "@/src/utils/queries/fetchAllProducts";

const allProducts = await fetchAllProducts();
---

<section
  class="px-4 sm:px-6 md:px-12 lg:px-[6rem] xl:px-[10rem] py-12 md:py-20"
>
  <div class="flex flex-col items-center text-center">
    <span
      class="inline-block bg-gray-200 text-gray-600 px-3 py-1 text-sm sm:text-sm font-bold rounded-full mb-4"
      >Soluciones completas
    </span>
    <h2 class="text-xl sm:text-2xl md:text-3xl font-bold mb-4">
      Nuestros Productos
    </h2>
    <p class="text-sm sm:text-base md:text-lg lg:text-xl max-w-4xl text-black">
      Ofrecemos una gama de chillers diseñados para optimizar tus sistemas de
      refrigeración y aire acondicionado. Con tecnología de vanguardia y
      eficiencia energética, nuestros chiller garantizan un funcionamiento
      fiable, reducción de costos operativos y un ambiente confortable
    </p>
  </div>

  <div
    class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-6 px-4 py-12 place-items-center"
  >
    {allProducts.map(({ acf }) => <ProductGrid {...acf} />)}
  </div>
</section>

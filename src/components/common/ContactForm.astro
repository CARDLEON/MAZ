---

---

<section class="py-12 px-4 max-w-6xl mx-auto">
  <p class="text-center text-base sm:text-lg md:text-xl font-medium mb-10">
    Completa el formulario a continuación con tus datos y mensaje,<br />
    y nos pondremos en contacto contigo lo antes posible.
  </p>

  <form
    action="https://formsubmit.co/0d8b09a3d7a82b190b0b6ed056162ed4 "
    method="POST"
    class="grid grid-cols-1 md:grid-cols-2 gap-6"
  >
    <!-- Columna izquierda -->
    <div class="space-y-6">
      <div>
        <label for="name" class="block text-lg font-semibold mb-2">
          Nombre Completo
        </label>
        <input
          id="name"
          type="text"
          name="name"
          required
          placeholder="Escriba su nombre"
          class="w-full border border-gray-400 rounded px-4 py-2 focus:outline-none focus:ring-2 focus:ring-blue-500"
        />
      </div>

      <div>
        <label for="email" class="block text-lg font-semibold mb-2">
          Correo electrónico
        </label>
        <input
          id="email"
          type="email"
          name="email"
          required
          placeholder="Escriba su correo electrónico"
          class="w-full border border-gray-400 rounded px-4 py-2 focus:outline-none focus:ring-2 focus:ring-blue-500"
        />
      </div>

      <div>
        <label for="phone" class="block text-lg font-semibold mb-2">
          Número Telefónico
        </label>
        <input
          id="phone"
          type="tel"
          name="phone"
          required
          placeholder="Escriba su número telefónico"
          class="w-full border border-gray-400 rounded px-4 py-2 focus:outline-none focus:ring-2 focus:ring-blue-500"
        />
      </div>
    </div>

    <!-- Columna derecha -->
    <div class="flex flex-col">
      <label for="message" class="text-lg font-semibold mb-2">Mensaje</label>
      <textarea
        id="message"
        name="message"
        rows={8}
        required
        placeholder="Déjanos tu mensaje"
        class="flex-grow border border-gray-400 rounded px-4 py-2 resize-none focus:outline-none focus:ring-2 focus:ring-blue-500"
      ></textarea>
    </div>

    <!-- hidden fields -->
    <input type="hidden" name="_next" value="http://maz.sandboxcardleon.com/thanks" />
    <input type="text" name="_honey" class="hidden" />
    <input type="hidden" name="_captcha" value="false" />

    <div class="md:col-span-2">
      <button
        type="submit"
        class="mt-4 bg-blue-900 hover:bg-blue-800 text-white font-semibold py-2 px-6 rounded transition"
      >
        Enviar mensaje
      </button>
    </div>
  </form>
</section>

---
import MainLayout from "@/src/layouts/MainLayout.astro";
import HeroSection from "@/src/components/hero/HeroSection.astro";
import ProjectsGrid from "@/src/components/grids/ProjectsGrid.astro";

import { gridProjects } from "@/src/utils/comps.config";
import { fetchHeroSections } from "@/src/utils/queries/fetchHeroSections";

const heroData = await fetchHeroSections("projects_hero_section");
---

<MainLayout title="MAZ - Descubre en qué tipo de proyectos te podemos apoyar!">
  <HeroSection {...heroData} />
  <ProjectsGrid projects={gridProjects} />
</MainLayout>
